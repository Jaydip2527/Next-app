import * as React from 'react';
import Leadkanban from "@/@/component/lead/kanban";

const LeadK = () => {
    return (
        <div className="lead_main_div">
            <Leadkanban />
        </div>
    );
};

export default LeadK;
